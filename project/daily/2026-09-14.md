

On passe a la creation de la nav

dans function `setPageRef` à partir de la ligne 130

panels = stack.querySelectorAll(".panel-card");
console.log("Building nav")

on veut associer  pour chaque panel on va stocker dans `pages`, 
`pages` est employé à un seul endroit pour le moment dans 
```js
let pages = ["Tableau de bord", "Paramètres", "Diagnostic"]
```

- index, titre dans pages[index] = titre  puis on utilisera un objet

```
        <div class="panel-card" data-index="0">
            <h2 class="panel-title">Schneider Modicon M221</h2>
```


```js
document.querySelector("#stack > div:nth-child(1)")
document.querySelectorAll("div.panel-card").forEach((card) => {
const panels = stack.querySelectorAll(".panel-card");
```

on doit améliorer voir [Améliorations/domhelper](#domhelper)




## Reference 
- stack => `_main`
- sidebar => `_menu`
- statusBar => `_footer` 

statusBar , themeBtn ,fullscreenBtn sont remplacés [Historique/2026-09-14/2026-09-14-001](#2026-09-14-001)
- statusBar => `_footer_status`
- themeBtn => `_header_actions_btn_theme`
- fullscreenBtn => `_header_actions_btn_fullscreen`

```html
<header id="header">
    <div class="header-actions">
        <button id="themeBtn" type="button">Thème nature</button>
        <button id="fullscreenBtn" type="button">Plein écran</button>
```



### fullscreenBtn

### sidebar => _menu
sidebar deviendra _menu, a voir si plusieurs nav dans l'idéal un seul donc 
utliser en parallele avant suppresion de sidebar
```js
sidebar = byId("sidebar", document) //actuel
// utliser en parallele avant suppresion de sidebar
//_menu = byName( "nav", document )
```

```
<nav id="sidebar">
```



### stack
  stack = document.getElementById("stack") => stack devient `_main`

2 endroits doient etre modifié intégralement

```js
   // _main = document.getElementById("stack")
    _main = byName("main")

    //console.error("uiapp.js : #stack ou #sidebar introuvable");
    console.error("uiapp.js : main ou #sidebar introuvable");
```


### statusBar

evolution on pourra utiliser footer pour plusieurs usages

```html
<!--
<footer id="statusBar">Automate actif : Schneider Modicon M221</footer>
-->

<footer>
    <div id="statusBar">
        Automate actif : Schneider Modicon M221
    </div> 
</footer>
```
on creer une variable `_footer`

```js
  _footer = byName("footer" , document )
```


### themeBtn



## Améliorations
### domhelper

// Événement sur les boutons des sous-onglets 
la function `initTabsButtonNaviagtion` doit utiliser panel

on, va importrer '/assets/js/core/domhelper.js'

```js
import { byId, byName , qs , qsa } from '/assets/js/core/domhelper.js'
```
#### intégration byId
- on remplace document.getElementById par byId
- on note chaque reference dans [Reference](#Reference)


### css
on va  coller à la structure, on utilise les classes  puis on deplace sur les element
et on utilise les couleurs ou en redefinir



```css
.header-actions button {
  padding: 6px 12px;
  margin-left: 8px;
  cursor: pointer;
  border-radius: 4px;
  border: none;
  background: rgba(255, 255, 255, 0.15);
  color: white;
}
/* ---------------  */
    :root[data-theme="marine"] {
    --bg-nav: #ffffff;  
    --accent: #2563eb;
    }
/* ---------------  */
    header > div.header-actions > button {

    }

```


## utilitaires

```js
/* on doit séparer gestion et affichage
ligne 79 : function `switchTab`
- statusBar.textContent = `Onglet actif : ${pages[index]}`;
+ statusWrite( `Onglet actif : ${pages[index]}` )
*/

statusWrite( textContent ){
    if (statusBar){ statusBar.textContent = textContent } else{ console.log("STATUS :: " + textContent) }    
}

// statusAppend() a préévoir ??
```


# Historique

## 2026-09-14

### 2026-09-14-000

- intégration de [domhelper](#domhelper)
- modification de la strucutre html : footer
- création des variables : `_main` , `_menu` , `_footer`
- test


resultat des test : byName renvoie HTMLCollection, il faut un HTMLElement pour employer `getElementById` , `querySelector` ou `querySelectorAll`

on a crée une fonction de test , `typeofObj` pour la suite
elle intégra domhelper par la suite

```js

  // _main = byName("main")
  // console.log( typeofObj(_main) )  
  
  _main = byName("main")[0]
  console.log( typeofObj(_main) )


function typeofObj( Obj ){
  let str = null
  if ( _main instanceof HTMLCollection ) { return "HTMLCollection" }
  if ( _main instanceof HTMLElement ) { return "HTMLElement" }
  return "Object"
}
```


d'autres utilisations de dom à voir
```js
    console.log(e.target.parentElement.nodeName) // The nodeName property returns the name of a node:
    console.log(e.target.parentElement.id) //The id property sets or returns the value of an element's id attribute.
    console.log(e.target.parentElement.className) //The className property sets or returns an element's class attribute.
    console.log(e.target.parentElement.classList) // classList property returns (DOMTokenList) the CSS classnames of an element
  /*
  classList Properties and Methods
    add() Adds one or more tokens to the list
    contains()  Returns true if the list contains a class
    entries() Returns an Iterator with key/value pairs from the list
    forEach() Executes a callback function for each token in the list
    item()  Returns the token at a specified index
    keys()  Returns an Iterator with the keys in the list
    length  Returns the number of tokens in the list
    remove()  Removes one or more tokens from the list
    replace() Replaces a token in the list
    supports()  Returns true if a token is one of an attribute's supported tokens
    toggle()  Toggles between tokens in the list
    value Returns the token list as a string
    values()  Returns an Iterator with the values in the list
  */
```

### 2026-09-14-001

on doit gérer les 3 variables et remplacer les anciennes

```js
  _footer = byName("footer" , document )
  // OBSOLETE a remplacer
  statusBar = document.getElementById("statusBar")
  themeBtn = document.getElementById("themeBtn")
  fullscreenBtn = document.getElementById("fullscreenBtn")
```

_footer_status
_header_actions_btn_theme
_header_actions_btn_fullscreen



### 2026-09-14-002

on remplace 
- getElementById
- querySelectorAll
- querySelector


#### getElementById
a été remplacé dans tous le fichier

```js
  // nav-article 
  /* nav#sidebar > div.nav-article > div.nav-header-row > a.nav-title */
  // sidebarList = document.getElementById("sidebarList"); //document.querySelector("#sidebar > div > div > a") 
  // sidebarList = document.querySelectorAll(".nav-article ")
```

#### querySelectorAll

```js
let panels = null
panels = _main.querySelectorAll(".panel-card");
```


```js
let sidebarList = null
sidebarList = document.querySelectorAll("nav#sidebar > div.nav-article > div.nav-header-row > a.nav-title")

sidebarList = document.querySelectorAll("ul.nav-toc > li > a")
```



##### initArticlesNavigation
```js
function initArticlesNavigation() {

  document.querySelectorAll('nav#sidebar > div.nav-article > div.nav-header-row').forEach((button) => {
    button.addEventListener('click', () => {
      const article = button.closest('.nav-article');
      const isOpen = article.classList.toggle('open');
      button.setAttribute( 'aria-expanded', isOpen ? 'true' : 'false' );
    });
  });

}
```


##### initArticleTabsNavigation
```js
function initArticleTabsNavigation() {

  document.querySelectorAll('ul.nav-toc > li > a').forEach((button) => {

    button.addEventListener('click', () => {
      const index = parseInt(button.dataset.targetId, 10)
      console.log(button.innerText + " " + index)
      switchTab(index)
      


      if (window.innerWidth > 768){                                         // pour pc
        const article = button.closest('.nav-article'); 
        const isOpen = article.classList.toggle('open');
        button.setAttribute( 'aria-expanded', isOpen ? 'true' : 'false' );
      }
      else{ closeSidebar() }                                                // pour mobile
    })      

  })

}
```
##### initTabsButtonNaviagtion
```js
// Événement sur les boutons des sous-onglets 
function initTabsButtonNaviagtion(){

  document.querySelectorAll("div.panel-card").forEach((card) => {

    const tabBtns = card.querySelectorAll("div.section-tab > div.tab-headers > button.tab-btn ");
    
    tabBtns.forEach(btn => {
      btn.addEventListener("click", () => {
    
        card.querySelectorAll(".tab-btn").forEach(b => b.classList.remove("active"));
        card.querySelectorAll(".tab-content").forEach(c => c.classList.remove("active"));
        
        btn.classList.add("active");
        card.querySelector(`#${btn.dataset.tab}`).classList.add("active");

      });
    
    });

  })

}
```

##### initPagination
```js
function initPagination() {

  document.querySelectorAll("div.pagination-buttons > button.switch-tab-btn").forEach((button) => {

    button.addEventListener("click", () => { 
      const index = parseInt(button.dataset.targetId, 10)
      switchTab(index); //switchPanel
    });

  
  })

}
```
##### switchTab
```js
// Changement d'onglet de navigation

function switchTab(index) {
  //const items = sidebarList.querySelectorAll("li");
  const panels = _main.querySelectorAll(".panel-card");

  sidebarList.forEach((item, i) => { item.classList.toggle("active", i === index)  })

  panels.forEach((panel, i) => { panel.classList.toggle("hidden", i !== index) })

  statusWrite( `Onglet actif : ${pages[index]}` )
}
```


## Conclusion

1. il faut poursuivre la migration des fonctions dom helper
   - définir des variables selon la convention _main , _footer_status        
2. supprimer le contenu de nav
   - les elements doivent etre lue par le dom, panels, tab-content
3. le styles doivent recoller à la structure , ajout de couleurs si besoin 

Pour constuire la nav on devra parcourir les panels et leur contenus

```js
  document.querySelectorAll("div.panel-card").forEach((card) => {

    const tabBtns = card.querySelectorAll("div.section-tab > div.tab-headers > button.tab-btn ");
    
    tabBtns.forEach(btn => {
      btn.addEventListener("click", () => {
    
        card.querySelectorAll(".tab-btn").forEach(b => b.classList.remove("active"));
        card.querySelectorAll(".tab-content").forEach(c => c.classList.remove("active"));
```

il faut continuer la fonction domhelper en exploitant  :
- https://developer.mozilla.org/fr/docs/Web/API/HTML_DOM_API
- https://developer.mozilla.org/fr/docs/Web/API/HTMLButtonElement
```js

function typeofObj( Obj ){
  let str = null
  if ( Obj instanceof HTMLButtonElement   ) { return "HTMLButtonElement  " }
  if ( Obj instanceof HTMLCollection ) { return "HTMLCollection" }
  if ( Obj instanceof HTMLDivElement ) { return "HTMLDivElement" }
  if ( Obj instanceof HTMLElement ) { return "HTMLElement" }
  return "Object"
}

```
