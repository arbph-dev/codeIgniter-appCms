2026-08-31-001-Etablissement



# Domaine Organisation
ajout des etablissments, table deja créé
- création du model [EtablissementModel.php](/refactoring/app/Models/EtablissementModel.php)
- création du controller [Etablissement.php](/refactoring/app/Controllers/Etablissement.php)
- 8 routes à déclarer

```php
// dans app/Config/Routes.php, groupe 'api'
$routes->get   ('etablissement',                    'Api\Etablissement::index');
$routes->get   ('etablissement/like',               'Api\Etablissement::like');
$routes->get   ('etablissement/(:num)',             'Api\Etablissement::show/$1');
$routes->post  ('etablissement',                    'Api\Etablissement::create');
$routes->put   ('etablissement/(:num)',             'Api\Etablissement::update/$1');
$routes->delete('etablissement/(:num)',             'Api\Etablissement::delete/$1');
$routes->get   ('organisation/(:num)/etablissements','Api\Etablissement::byOrganisation/$1');
$routes->post  ('organisation/(:num)/etablissement','Api\Etablissement::siege/$1');
```


Logique métier respectée

Le NIC est dérivé automatiquement du siret (substr 9–14) si tu ne le fournis pas, cohérent avec ce que fait déjà ensureSiege dans le service.

La route POST /organisation/:id/etablissement délègue à EntrepriseService::ensureSiege() 
elle gère la désactivation des anciens sièges, la vérification SIRET/SIREN et l'upsert. 

Ton ORBIS menu 9 → fiche entreprise passera par loadFull() qui appelle ce même service, donc l'erreur EtablissementModel not found disparaît dès que le model est en place.

**Point A** :
- Le delete() du controller ne fait pas de soft delete (la table n'a pas de deleted_at), juste un delete direct.

**Point B** :
- Dans EtablissementModel, withRelations() fait un join sur adresses avec les colonnes :
  - libelle
  - code_postal
  - ville
  - pays.
  
Si le schéma adresses a des noms différents (ex. rue, complement, cp…), ajuste juste le select() : [withRelations()](/refactoring/app/Models/EtablissementModel.php#L50)



- [ ] Point A : a voir avant ajout champ deleted_at
- [ ] Point B : voir le schéma Adresse

