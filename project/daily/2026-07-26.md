# Itération 005 — Refactoring stack CMS (2026-07-26)

## Contexte
Suite à la revue complète de la stack (Iter005 de `2026-07-25.md`),  
six points de fragilité ont été identifiés. Cette itération traite les quatre premiers.

---

## Stack actuelle (rappel)

```
URL /cms/article/test-art
  → Route CI4 : CmsController::article($slug)
  → CmsController retourne view('cms/article2', [ article, content ])
  → article2.php : structure HTML + import CmsArticleWorkbench
  → CmsArticleWorkbench.loadFromPHP(article, content)
      → setupComponentRegistry()
      → initRegisteredComponents()   ← chaque composant initialisé
```

---

## Points traités

### 1. Gestion du bus

**Avant (Iter004)**  
`WorkbenchBase` cherchait le bus dans `config.eventBus || window.bus || null`.  
Fragile : dépend de l'ordre de chargement des scripts et de globals.

**Après (Iter005)**  
```js
// WorkbenchBase.js
import { bus } from '/assets/js/core/eventBus.js';
// ...
this.bus = bus;   // singleton importé directement
```

Façade exposée aux classes filles :
```js
this.publish('cms:article:loaded', { slug });
this.subscribe('cms:section:change', (data) => { ... });
```

`window.eventBusPublish` (héritage bootstrap.js) n'est plus nécessaire.

---

### 2. Gestion des composants — suppression de bootstrap.js

**Avant (Iter004)**  
`setupComponentRegistry()` enregistrait une seule fonction `initCms`  
qui appelait `bootstrap.js/initCms()` — fichier d'essai, couplage fort.

**Après (Iter005)**  
Chaque composant est importé et enregistré individuellement :

```js
// CmsArticleWorkbench.js
import { initApex }    from '/assets/js/components/apex.js';
import { initCallout } from '/assets/js/components/callout.js';
import { initCodeVal } from '/assets/js/components/codeval.js';
import { initLeaflet } from '/assets/js/components/leaflet.js';
import { initMermaid } from '/assets/js/components/mermaid.js';
import { init as initThree } from '/assets/js/components/three/index.js';

setupComponentRegistry() {
    this.register('apex',    initApex);     // ① dépendance codeval
    this.register('callout', initCallout);  // ② indépendants
    this.register('leaflet', initLeaflet);
    this.register('mermaid', initMermaid);
    this.register('three',   initThree);
    this.register('codeval', initCodeVal);  // ③ consommateur d'events
}
```

`bootstrap.js` n'est plus importé.  
L'ordre apex → codeval est préservé (contrainte documentée dans bootstrap.js).

---

### 3. Intégration de domhelper

**Avant (Iter004)**  
`getElement()` utilisait `this.domHelper` (config externe optionnel) ou  
`this.container.querySelector()` brut.

**Après (Iter005)**  
```js
// WorkbenchBase.js
import { qs, qsa, byId, create, clear, toggle } from '/assets/js/core/domhelper.js';

// exposés aux classes filles via this.dom
this.dom = { qs, qsa, byId, create, clear, toggle };

// getElement() utilise qs importé
getElement(selector) {
    return qs(selector, this.container);
}
```

Prochaine étape : `templates.js` et `validation.js` selon le même pattern.

---

### 4. TabSystem — nouveau composant

**Fichier** : `assets/js/ui/workbench/TabSystem.js`
  https://github.com/arbph-dev/codeIgniter-appCms/blob/main/refactoring/assets/js/ui/workbench/TabSystem.js

Système d'onglets générique, indépendant du CMS.

#### API

| Méthode | Description |
|---------|-------------|
| `addTab(id, label, renderFn, initFn?)` | Ajoute un onglet. Chaînable. |
| `activate(id)` | Active un onglet. Init paresseuse (initFn une seule fois). |
| `render(container)` | Construit nav + panes. Active le premier onglet. |
| `updateTabContent(id, content)` | Met à jour un pane (HTMLElement ou string). |
| `setBadge(id, text)` | Ajoute un badge/compteur au bouton nav. |
| `destroy()` | Vide et libère. |

#### Exemple — sections d'article

```js
// Dans CmsArticleWorkbench (Iter006+)
loadSections(sections) {
    const tabs = this.initTabSystem();
    const container = this.getElement('#wb-content');

    sections.forEach(section => {
        tabs.addTab(
            `section-${section.id}`,
            section.title,
            () => {
                const div = this.dom.create('div', { class: 'cms_section' });
                div.innerHTML = section.content;
                return div;
            },
            (pane) => {
                // init composants de la section au premier affichage
                this.initRegisteredComponents();
            }
        );
    });

    tabs.render(container);
}
```

#### CSS à ajouter dans `workbench.css`

```css
.wb_tabs_nav        { display: flex; gap: 4px; border-bottom: 2px solid var(--wb-border); }
.wb_tab_btn         { padding: 6px 14px; background: none; border: none; cursor: pointer; }
.wb_tab_btn.active  { border-bottom: 2px solid var(--wb-accent); font-weight: bold; }
.wb_tab_pane        { padding: 12px 0; }
.wb_tab_badge       { margin-left: 6px; background: var(--wb-accent); border-radius: 10px;
                       padding: 1px 6px; font-size: 0.75em; }
```

---

## Fichiers modifiés / créés

| Fichier | Action | Iter |
|---------|--------|------|
| `assets/js/ui/workbench/WorkbenchBase.js` | Modifié | 005 |
| `assets/js/ui/workbench/TabSystem.js` | **Créé** | 005 |
| `assets/js/ui/workbench/layouts/CmsArticleWorkbench.js` | Modifié | 005 |

---

## Points restants (prochaines itérations)

### Iter006 — Navigation par sections
- Intégrer `TabSystem` dans `CmsArticleWorkbench` pour afficher une section par onglet
- Relier `/cms/section/:id` API → `TabSystem.updateTabContent()`
- Revoir le panel debug (treeview JSON prévu en Iter004)

### Iter007 — Librairies core
- `templates.js` → intégrer via `this.templates` dans WorkbenchBase  
- `validation.js` → exposer `PropertySet` / `ComputePropertySet` pour les features
- `apiFetch.js` → façade `this.fetch()` dans WorkbenchBase (auth token automatique)

### Iter008 — CmsView
- Workbench `cmstree + cmspart/edit + cms/part` ensemble
- Réécriture `wysedit.js` → `components/wysedit.js`

### Suivant — Composants à réintégrer
`carousel`, `features`, `voice`, `scene`

### Suivant — API
- Séparation claire MVC vs API REST  
- Réflexion CURL pour INPI / INSEE depuis le Service CodeIgniter



# Iteration 006 — Navigation par sections

## Iter006.1 - En cours
Intégrer TabSystem dans CmsArticleWorkbench pour afficher une section par onglet
- [X] ajouter CSS dans `/assets/css/components/workbench.css`
  https://github.com/arbph-dev/codeIgniter-appCms/blob/main/refactoring/assets/css/components/workbench.css

**`TabSystem.js`** — une seule correction mais critique : 
- `tab.initialized = true` est positionné **avant** l'appel à `initFn()`.
  Sans ça, un double-clic rapide pendant un fetch déclenche deux requêtes parallèles sur la même section. En cas d'erreur (sync ou async), `initialized` est remis à `false` pour permettre un retry.

**`CmsArticleWorkbench.js`** — le `loadFromPHP()` branche maintenant automatiquement :
- `article.sections.length > 1` → mode onglets :
  `loadSections()` construit le TabSystem, le `render()` active le premier onglet, ce qui déclenche immédiatement le premier `fetchSection()`
- sinon → mode plat (comportement Iter005, `$content` PHP direct)

Le paramètre `content` reste passé mais inutilisé en mode tabs — c'est intentionnel, il sert de fallback si un jour les sections ne sont pas disponibles.

`getTabs()` expose le TabSystem pour activation programmatique depuis l'extérieur (`wb.getTabs()?.activate('section-3')`).

**`CmsController.php`** — une seule ligne ajoutée : si `$article['sections']` est vide, appel à `$this->cms->getArticleSections($article['id'])`. **CmsService doit implémenter cette méthode** avec le format `[{ id, title, position }, …]`.

---

**Point d'attention CSS** : les vars `--wb-border` et `--wb-accent` sont scopées à `#modelworkbench`. Dans la page article elles ne résolvent pas. Ajouter dans `workbench.css` ou `article.css` :

css

```css
:root {
    --wb-border : #30363d;
    --wb-accent : #eee236;
}
```


**Priorité 1 — CmsController + CmsService**

J'ai déjà `CmsController.php`. Il me faut :

- `app/Services/CmsService.php` — pour voir les méthodes existantes (`getPublishedArticle`, `renderArticle`, `renderSection`, `getArticleSections` si elle existe déjà)
- Les modèles si séparés : `ArticleModel.php`, `SectionModel.php`, `PartModel.php` (ou équivalents)

---

**Priorité 2 — Migration contenu + cmsparts**

Il me faut :

- Le controller admin qui gère les parts : probablement `app/Controllers/Admin/CmsPartController.php` ou similaire
- La vue de l'éditeur de part (wysedit ou form) : `app/Views/admin/cmspart/`
- Le schéma de la base ou les migrations CI4 pour les tables `cms_articles`, `cms_sections`, `cms_parts` — même un dump partiel suffit
- `assets/js/ihm/wysedit.js` (ou son emplacement actuel) mentionné dans les notes

---

**Priorité 3 — Registry / init ciblée par container**

Il me faut les composants actuels :

- `assets/js/components/apex.js`
- `assets/js/components/callout.js`
- `assets/js/components/codeval.js`
- `assets/js/components/leaflet.js`
- `assets/js/components/mermaid.js`
- `assets/js/components/three/index.js`

La direction est claire : chaque `initXxx(root = document)` reçoit un container optionnel, `querySelectorAll` s'applique sur `root` au lieu de `document`. Ça maintient la compatibilité descendante et permet l'init ciblée par pane.

### Itération 006.2.1 : analyse de app/Services/CmsService.php


#### Diagnostic immédiat

`getArticleSections()` évoquée en Iter006.1 **n'existe pas**. La méthode réelle s'appelle **`getSectionsByArticle(int $articleId)`** — elle est déjà là.

→ Le `CmsController.php` produit en Iter006.1 doit être corrigé : 1 ligne. → `CmsService.php` n'a pas besoin d'être modifié pour Iter006.1.

---

#### Carte des méthodes utiles pour le TabSystem

##### Méthodes clés — lecture

|Méthode|Retourne|Notes|
|---|---|---|
|`getPublishedArticle(slug)`|`?array`|Article seul, **sans sections**|
|`getSectionsByArticle(id)`|`array`|Sections d'un article, triées par position, `is_published = 1`|
|`getFullArticle(slug)`|`array`|Article + sections + parts (arbre complet)|
|`getArticleTree(slug)`|`array`|Article + sections **sans parts** — mais utilise `getArticle()` (pas de filtre `is_published`) ⚠|
|`renderSection(id)`|`string`|HTML fragment → parfait pour `fetchSection()`|
|`renderArticle(slug)`|`string`|HTML de toutes les sections — utilisé en mode plat|
|`getSectionsByArticle()`|`array`|= ce que j'ai appelé `getArticleSections()` en Iter006.1|

---

#### Flux de données — mode tabs vs mode plat

```
CmsController::article($slug)
  │
  ├─ getPublishedArticle($slug)           → $article (sans sections)
  ├─ getSectionsByArticle($article['id']) → $article['sections']   ← AJOUT Iter006.1
  └─ renderArticle($slug)                 → $content  (HTML complet, fallback mode plat)
        └─ getFullArticle()               (recharge article + sections + parts)
              └─ getSectionsByArticle()
              └─ getPartsBySection() × N
```

**Observation :** `renderArticle()` recharge l'article via `getFullArticle()`. Il y a donc une double lecture en mode tabs (article lu deux fois). Acceptable pour l'instant — à optimiser si besoin via un cache ou en passant l'objet.

---

#### Ce que le JS reçoit dans `article.sections`

`getSectionsByArticle()` retourne pour chaque section les colonnes de la table, soit au minimum :

```js
article.sections = [
    { id: 1, article_id: 5, slug: 'intro',   title: 'Introduction',  position: 1, is_published: 1 },
    { id: 2, article_id: 5, slug: 'dev',     title: 'Développement', position: 2, is_published: 1 },
    { id: 3, article_id: 5, slug: 'conclusion', title: 'Conclusion', position: 3, is_published: 1 },
]
```

Le `TabSystem` n'a besoin que de `id`, `title`, `position` → c'est exactement ce qu'il reçoit. Les `parts` ne sont **pas** incluses ici, contrairement à `getFullArticle()` — c'est voulu : on ne charge les parts que quand l'onglet est activé (via `fetchSection()`).

---

#### `renderSection()` — compatibilité avec `fetchSection()`

```php
public function renderSection(int $sectionId): string
{
    $section = $this->sections->find($sectionId);
    if (!$section) { return ''; }
    return $this->renderSectionData($section, $this->getPartsBySection($sectionId));
}

private function renderSectionData(array $section, array $parts): string
{
    $html = '';
    foreach ($parts as $part) {
        $html .= $this->renderPart($part);  // view('cms/part', ...)
    }
    return view('cms/section', ['section' => $section, 'content' => $html]);
}
```

`renderSection()` retourne le résultat de `view('cms/section', ...)` — un **fragment HTML** sans `<html>/<head>`. ✅ C'est exactement ce que `fetchSection()` attend pour l'injection.

---

#### Correction CmsController.php

##### Avant (Iter006.1 — méthode inventée)

```php
if (empty($article['sections'])) {
    $article['sections'] = $this->cms->getArticleSections($article['id'] ?? null) ?? [];
}
```

##### Après (Iter006.2.1 — méthode réelle)

```php
$article['sections'] = $this->cms->getSectionsByArticle($article['id']);
```

Pas de `if (empty(...))` — on assigne toujours, c'est plus propre. Si l'article n'a pas de sections, `getSectionsByArticle()` retourne `[]`. Le JS fait déjà `sections.length > 1` → bascule en mode plat sans erreur.

---

#### Points relevés pour les prochains chantiers

##### Chantier 2 — Migration contenu / cmsparts

`getPartsBySection()` délègue à `$this->parts->findBySection()` :

```php
public function getPartsBySection(int $sectionId, bool $includeUnpublished = false): array
{
    return $this->parts->findBySection($sectionId, $includeUnpublished);
}
```

→ `CmsPartModel` a une méthode custom `findBySection()`. Il faudra ce fichier.

`newPart()`, `createPart()`, `updatePart()`, `deletePart()`, `movePartUp/Down()` sont tous dans CmsService — c'est le CRUD complet. Bien structuré.

`enrichPart()` ajoute `type_name`, `type_label`, `type_icon`, `type_class` à partir du catalogue `ComponentTypeModel`. Utilisé dans `getCmsTree()`.

##### Chantier 3 — Registry / init ciblée

`renderPart()` appelle `$this->renderer->render($descriptor)` via `ComponentRenderer`. La chaîne est : `Part → DescriptorMapper → descriptor → ComponentRenderer → HTML`. Pour l'init ciblée côté JS, il faudra les 6 fichiers composants.

---

###### Fichiers encore nécessaires

|Priorité|Fichier|Pour quoi|
|---|---|---|
|①|`CmsPartModel.php`|Voir `findBySection()`, schéma colonnes|
|①|`app/Views/cms/section.php`|Vérifier la structure HTML retournée par `renderSection()`|
|②|`CmsArticleModel.php`, `CmsSectionModel.php`|Colonnes disponibles en base|
|②|Admin controller cmspart|Pour la migration/édition des parts|
|③|`assets/js/components/*.js` (6 fichiers)|Chantier init ciblée|
---

## Iter006.2
Relier /cms/section/:id API → TabSystem.updateTabContent()

## Iter006.2
Revoir le panel debug (treeview JSON prévu en Iter004)


## Évolution librairies
- Remplacement apex → **Plotly** (étudier migration)
- Remplacement mermaid → alternative légère si besoin

---



