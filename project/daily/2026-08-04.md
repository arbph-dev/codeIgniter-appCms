


Le contrat Panel (render / show / clear / destroy) est proprement tenu dans les deux panels. C'est ce qui permet au Workbench d'être un pur assembleur — aucune logique d'affichage ne s'y glisse, et ça se voit. La séparation est franche.

Le namespacing wb:mot:page sur le bus est une bonne discipline : quand plusieurs Workbenches cohabiteront, il n'y aura pas de collision. Idem pour les classes CSS wb_mot_*.

Le mix callbacks (panel → workbench) / bus (pagination → workbench) est cohérent : les callbacks restent locaux à l'instance, le bus ne sert qu'à ce qui traverse les frontières.

Ce qui mérite attention avant d'avancer sur le CRUD

destroy() appelle this.bus.unsubscribe?.('wb:mot:page') sans passer la référence du callback. Selon l'implémentation du bus, ça peut ne rien désabonner et laisser un listener zombie. À vérifier ou à corriger avant de multiplier les instances.

Le bug fetchMot sans q est documenté dans le code, ce qui est bien, mais il est aussi actif : la liste initiale ne pagine pas correctement. C'est un fix simple et il bloquera le CRUD dès qu'on voudra tester la pagination après un ajout. Autant le solder maintenant.

Ce qui peut attendre

WorkbenchBase.renderStructure() génère un .wb-header que le CSS de motWorkbench cache immédiatement avec display: none. Ce n'est pas grave en Step 1 mais ce sera à nettoyer quand le header aura un contenu réel.

Le stub CSS dans mot.php duplique des règles de workbench.css. C'est un choix de confort pour le développement, rien à redire pour l'instant.

En résumé

L'architecture tient la route et le rythme incrémental (Steps 1/2/3 documentés dans les commentaires) est une bonne façon de ne pas sur-ingénierer. Le seul vrai point de fragilité avant le CRUD c'est le unsubscribe — si les Workbenches peuvent s'ouvrir et se fermer plusieurs fois, ce leak peut devenir désagréable à debugger.

----

mot.service.js

Le fix fetchMot est propre. La séparation id / liste est claire, page et per_page partent toujours quand c'est une liste. 
Le commentaire dans MotWorkbench peut être retiré, il ne documente plus un bug mais un état résolu.

fetchMotLike — la version 002 est correcte mais le code de la 001 commentée devrait être supprimé plutôt que conservé. 
Le bus.publish commenté dans la 001 est révélateur d'une hésitation architecturale : le service a failli prendre en charge de l'UI. C'est bien que cette voie ait été abandonnée — le service ne doit pas connaître le bus.

Un point de fragilité : fetchMotLike avale silencieusement toutes les erreurs réseau (catch { return [] }). 
C'est un choix délibéré pour de l'autocomplétion — acceptable — mais si cette fonction est un jour réutilisée dans un contexte où l'erreur compte, ce comportement peut surprendre. Un commentaire explicite du choix serait utile.

saveMot est bien écrit, la bascule POST/PUT sur id est lisible. Le commentaire sur le 422 mérite d'être remonté dans MotWorkbench plutôt que dans le service : c'est le Workbench qui devra interpréter ce code et afficher un message utilisateur ("ce mot existe déjà"), pas le service qui doit juste propager l'erreur.

Ce qui veut dire que dans load() et dans le futur handler de sauvegarde, il faudra distinguer :

```js
catch (err) {
    if (err.message.includes('422')) {
        // feedback utilisateur "mot déjà existant"
    } else {
        this.listPanel.showError(err.message)
    }
}
```





Pour le CRUD à venir, le service est prêt. 
La séquence naturelle dans MotWorkbench sera : 
appel saveMot → load() pour rafraîchir la liste → detailPanel.show() avec le mot retourné par l'API. 

Pas besoin d'optimistic update pour commencer.

---


1. mot.service.js — retirer le bloc fetchMotLike 001 commenté ; commenter le silence du catch explicitement

2. MotListPanel — bouton + Nouveau dans le header + callback onNew + init/destroy

3. MotDetailPanel — Step 2 complet :

_feedbackEl persistant (au-dessus du body, indépendant de son contenu)
show() : détail + boutons Modifier / Supprimer
showNew() : formulaire création
_showForm(mot) : unique formulaire partagé create/edit (Enter + Escape)
_confirmDelete(mot) : confirmation inline
lock() / unlock() : protection anti double-submit
onSave(fn) / onDelete(fn) / showFeedback(type, msg)

4. MotWorkbench — import saveMot/deleteMot ; _onPageFn stocké → destroy() sans zombie ; handlers onNew/onSave/onDelete ; gestion 422 ; _page = 1 sur create et delete ; retrait commentaire bug fetchMot obsolète

5. workbench.css — bloc à appender : feedback error/success, danger, form, confirm, Nouveau btn



Points à noter une fois en prod :

result.data après saveMot — 

Request URL : https://zealot.fr/api/mot
Request Method : POST
Request Payload :{"mot_lbl":"hystérétiques"}


à vérifier contre la réponse réelle du controller ; le fallback { mot_id: id, mot_lbl: lbl } couvre le cas où l'API renvoie juste { status: 'ok' }

{
    "status": 201,
    "message": "Mot « hystérétiques » créé.",
    "data": {
        "mot_id": "336877",
        "mot_lbl": "hystérétiques"
    }
}



La ligne sélectionnée dans la liste n'est pas marquée .is-selected — le CSS existe déjà, c'est un Step 4 naturel qui demande d'exposer selectedId dans la factory table()

Le unsubscribe suppose que le bus accepte (event, fn) — à adapter si l'implémentation diffère

---
---

# Stage 2.1 — Refactoring de l'infrastructure Workbench

Extraire progressivement les responsabilités de CmsArticleWorkbench afin d'obtenir une architecture de composition commune à tous les Workbench.
Le point clé de cette étape est un changement de paradigme : le Workbench ne construit plus directement son interface, il exécute une composition décrite par un LayoutDescriptor et matérialisée par une WorkbenchView. Les Panels deviennent des briques d'interface réutilisables, les templates et la validation sont mutualisés, et CmsArticleWorkbench peut être démantelé progressivement sans rupture. Cette approche reste fidèle au principe du projet : une architecture simple, lisible et évolutive, où chaque couche possède une responsabilité clairement délimitée.
Le Stage 2.1 ne modifie pas les fonctionnalités existantes.

Il prépare les futurs Workbench :

KnowledgeWorkbench
ComponentCatalogWorkbench
ImageWorkbench
ImageTaggerWorkbench
CarouselWorkbench
MathGraphWorkbench
SceneWorkbench
ModelWorkbench
État actuel

Architecture validée :
```
Workbench
    ↓
Service
    ↓
apiFetch()
    ↓
API
```

Panels :
render()
show()
clear()
destroy()

Workbench :
bootstrap()
createLayout()
createPanels()
bindEvents()
load()
destroy()

MotWorkbench devient la référence de l'architecture actuelle.

## Axe 1 — Décomposition de CmsArticleWorkbench

Responsabilités actuelles :
CmsArticleWorkbench
• layout
• chargement
• navigation
• tabs
• sections
• composants
• appels API

Objectif :
```
Workbench
↓
WorkbenchView
↓
Panels
↓
Components
```

Workbench
Conserve :
bootstrap()
load()
refresh()
destroy()
bindEvents()

Fichier : assets/js/ui/workbench/core/WorkbenchBase.js


### WorkbenchView (nouveau)

Responsabilités :

construire le layout
gérer les zones
créer les Panels
gérer les templates
gérer les formulaires
gérer les validations UI
orchestrer les Sections

Fichier : assets/js/ui/workbench/core/WorkbenchView.js

### LayoutDescriptor
Décision : Un unique LayoutDescriptor est introduit.

Il décrit :
les zones
les Panels
la composition

Il ne décrit jamais le HTML.Une famille complète de descripteurs reste une évolution future.

Fichier :assets/js/ui/workbench/core/LayoutDescriptor.js

## Axe 2 — Centralisation des Templates

Décision :Les templates ne seront plus construits dans les Panels. Ils seront centralisés dans la WorkbenchView.

Objectifs :
éviter les duplications
uniformiser les layouts
permettre plusieurs vues d'un même Workbench

Structure proposée :
```
assets/js/ui/shared/
    templates/
        detail.template.js
        form.template.js
        list.template.js
        toolbar.template.js
```

## Axe 3 — Validation des formulaires

La validation ne doit plus être répartie dans chaque Panel.

Créer une ressource commune.

Structure proposée :
```
assets/js/ui/shared/
    validation/
        validator.js
        rules.js
```

Responsabilités :
required
minlength
maxlength
pattern
messages
validation temps réel
validation submit

Les Panels restent responsables uniquement des callbacks.

## Axe 4 — Form

Créer un composant commun.
```
assets/js/ui/shared/
    Form.js
```

Responsabilités :
génération des champs
binding
validation
récupération des valeurs

Les formulaires de MotWorkbench deviendront le premier cas d'utilisation.

## Axe 5 — Dispatcher CmsArticleWorkbench

Extraction progressive :
```
CmsArticleWorkbench
↓
WorkbenchBase
↓
WorkbenchView
↓
LayoutDescriptor
↓
Panels
↓
ComponentRegistry
↓
Services
```

Aucune régression fonctionnelle. Le code sera déplacé progressivement.

## Axe 6 — Structure cible
```
assets/js/
├── core/
│   ├── apiFetch.js
│   ├── eventBus.js
│   ├── domhelper.js
│   ├── store.js                 (futur)
│   └── utils/
├── features/
│   ├── mot/
│   │   └── mot.service.js
│   ├── image/
│   │   ├── image.service.js
│   │   └── image.mapper.js
│   ├── component/
│   │   ├── component.service.js
│   │   └── component.mapper.js
├── components/
│   ├── apex/
│   ├── codeval/
│   ├── mermaid/
│   └── three/
└── ui/
    ├── workbench/
    ├── core/
    │   ├── WorkbenchBase.js
    │   ├── WorkbenchView.js        (nouveau)
    │   ├── WorkbenchStore.js       (futur)
    │   └── LayoutDescriptor.js     (nouveau)
    ├── panels/
    │   └── PanelBase.js            (futur)
    ├── runtime/
    │   ├── MotWorkbench.js
    │   ├── ImageWorkbench.js
    │   ├── SceneWorkbench.js
    │   └── KnowledgeWorkbench.js
    ├── builder/
    │   ├── ComponentCatalogWorkbench.js
    │   ├── CarouselWorkbench.js
    │   └── MathGraphWorkbench.js
    ├── views/
    │   ├── DescriptorPanel.js
    │   ├── JsonPanel.js
    │   └── DefinitionPanel.js
    └── shared/
        ├── Table.js
        ├── Form.js
        ├── Modal.js
        ├── templates/
        │   ├── detail.template.js
        │   ├── form.template.js
        │   ├── list.template.js
        │   └── toolbar.template.js
        └── validation/
            ├── validator.js
            └── rules.js
```

# Evolutions futures
Séparation complète bus global / communication interne des Panels.
WorkbenchStore.
Famille de descripteurs (WorkbenchDescriptor, PanelDescriptor, ComponentDescriptor).
Sélection persistante dans les tables (selectedId).
Réintégration de l'authentification.
Layouts dynamiques pilotés par configuration.
Réflexion d'architecture

