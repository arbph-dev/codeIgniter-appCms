Ordre de travail

Je propose donc :

1. ensureLeaflet()
        ↓
2. préserver initLeaflet(root)
        ↓
3. support explicite Workbench
        ↓
4. gestion visibilité
        ↓
5. sécurisation renderMap()
        ↓
6. destruction / réinitialisation
        ↓
7. tests cartes statiques + AdresseWorkbench
Et surtout : pas de refonte

On conserve :

MAPS
instances
renderMap()
destroyMap()
updateMap()
bootstrapFromDOM()
les événements leaflet:render/update/destroy/list
les API window.leafletRender/...

La première modification doit être petite et isolée : extraire l'initialisation des abonnements du scan DOM. C'est celle qui nous donnera la base propre pour intégrer AdresseWorkbench sans toucher au Leaflet existant.

---


On y va, **étape 1 uniquement** : séparer l’initialisation du moteur Leaflet du scan du DOM.

### Modification de `leaflet.js`

On extrait les `bus.subscribe(...)` dans `initLeafletEngine()` et on ajoute `ensureLeaflet()`.

```js
/* =============================================================================
   5. INDEX
   ========================================================================= */

let _initialized = false   // guard : bus souscrit une seule fois


/**
 * Initialise uniquement le moteur Leaflet :
 * - abonnement aux événements du bus
 * - aucune recherche dans le DOM
 * - aucune création de carte
 *
 * Utile pour les Workbench dont les cartes sont créées explicitement
 * via leaflet:render.
 */
function initLeafletEngine()
{
    if (_initialized) return

    bus.subscribe('leaflet:render', ({ id, type, payload = {} }) => {
        const builder = MAPS[type]
        if (!builder) {
            console.warn(`[leaflet] type inconnu "${type}"`)
            return
        }
        renderMap(id, builder(payload))
    })

    bus.subscribe('leaflet:update', ({ id, payload = {} }) =>
        updateMap(id, buildOsmConfig(payload))
    )

    bus.subscribe('leaflet:destroy', id =>
        destroyMap(id)
    )

    bus.subscribe('leaflet:list', () =>
        listMaps()
    )

    _initialized = true
    console.log('[leaflet] initialisé')
}


/**
 * Garantit que le moteur Leaflet est initialisé.
 *
 * IMPORTANT :
 * cette fonction ne scanne pas le DOM et ne crée aucune carte.
 */
export function ensureLeaflet()
{
    initLeafletEngine()
}


/**
 * Initialise Leaflet puis découvre les cartes statiques
 * présentes dans root.
 *
 * Compatibilité descendante :
 * initLeaflet() continue donc à fonctionner comme auparavant.
 */
export function initLeaflet(root = document)
{
    initLeafletEngine()
    bootstrapFromDOM(root)
}
```

### Ce que cela change

Avant :

```js
initLeaflet()
```

provoquait :

```
abonnement bus
     +
scan DOM
     +
création éventuelle des cartes
```

Maintenant :

```js
ensureLeaflet()
```

provoque uniquement :

```
abonnement bus
```

Alors que :

```js
initLeaflet(section)
```

continue de provoquer :

```
abonnement bus si nécessaire
        +
scan de section
        +
rendu des .cp_leaflet
```

Donc **aucune modification nécessaire dans `uiapp.js`**.

---

### Test attendu avant d'aller plus loin

Dans `AdresseWorkbench.js`, on pourra temporairement remplacer :

```js
import { initLeaflet } from '/assets/js/components/leaflet.js'
```

par :

```js
import { ensureLeaflet } from '/assets/js/components/leaflet.js'
```

et :

```js
initLeaflet()
```

par :

```js
ensureLeaflet()
```

Le Workbench pourra alors s'initialiser **sans demander à Leaflet de créer une carte dans un conteneur caché**.

Les cartes statiques doivent, elles, continuer à passer par :

```js
initLeaflet(panel_Sections[index])
```

dans `switchSection()`.

**On ne fait rien d'autre pour l'instant.** Une fois ce test validé, on attaque l'étape 2 : faire apparaître la carte du `MapPanel` uniquement lorsque le Workbench est réellement visible.
