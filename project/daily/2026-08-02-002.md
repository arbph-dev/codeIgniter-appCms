2026-08-02-002 : 

/assets\js\core\ComponentCatalog.js



1) ComponentCatalog.loadDefaults()

Aujourd'hui il est encore vide. 
Il était prévu pour fonctionner avec ComponentDefinitionRegistry pour enregistrer les definitions des composants.
Les definitons des composant ssont stockées dans assets/js/core/ComponentDefinition.js
Mais j'ai pu avancer sur la partie php

```
catalog.register(

    new ComponentDefinition({
        type : 'apex',
        ...
    })
);
```

ComponentDefinition
```
    "type": "apex",
    "title": "Apex Charts",
    "description": "Interactive charts",
    "category": "Charts",
    "version": "1.0.0",
    "author": "Zealot",
    "icon": "fa-chart-line",
    "tags": [
        "chart",
        "visualization"
    ],

```



-----------

 
On cherche a intégrer la gestion de la plateforme de connaissance 
le ComponentCatalogWorkbench doit maintenant proposer le catalogue depuis les données du serveur php codeigniter 

----
ComponentTypeTest permet de vérifier les données avant de mettre en place la lecture côté client 
    https://zealot.fr/cmptest
```
$routes->get('cmptest', 'ComponentTypeTest::index');
```
 
[app/Controllers/ComponentTypeTest.php](/refactoring/app/Controllers/ComponentTypeTest.php)
 
```
// Étape 1 : Chargement du modèle
// Étape 2 : findActive()
// Étape 3 : findByName()
// Étape 5 : ComponentCatalog
$catalog = new \App\Libraries\Components\ComponentCatalog();
// Étape 6 : DescriptorMapper
$mapper = new \App\Libraries\Components\DescriptorMapper();
```


# définitions de composants

Les définitions de composants sont stockés dans la table component_types et doit être harmonisé avec le coté client
    app/Libraries/Components/ComponentDefinition
Les fichiers JavaScript suivant doivent permettre de géré ces définitions     
    assets/js/core/ComponentDefinition.js

## [app/Models/ComponentTypeModel.php](/refactoring/app/Models/ComponentTypeModel.php)
```
class ComponentTypeModel extends Model
{
    protected $table      = 'component_types';
    protected $primaryKey = 'id';
    protected $returnType = 'array';

    protected $allowedFields = [
        'name',
        'view',
        'description',
        'is_active',
    ];

    /** Retourne tous les types actifs.    */
    public function findActive(): array

   /** Retourne un type à partir de son nom.  */
    public function findByName(string $name): ?array

    /** Retourne la correspondance id => name. */
    public function getTypeMap(): array
}
```




## [app/Libraries/Components/ComponentDefinition.php](/refactoring/app/Libraries/Components/ComponentDefinition.php)

Définition d'un composant. Cette classe décrit les métadonnées techniques d'un composant.
```
final class ComponentDefinition
{
    public function __construct(
        public string  $type,
        public string  $description,
        public string  $label,
        public string  $icon,
        public string  $cssClass,
        public string  $descriptorClass,
        public string  $rendererClass,
        public string  $adminRendererClass,
        public array   $resources = [],
        public array   $features = [],
        public array   $connectors = [],
        public ?string $workbenchClass = null,
        public array   $metadata = [],
    ) {}
}
```
 

 


# gestion CRUD
 
Le crud est géré 

 
```
$routes->group('admin/component-types', static function ($routes)
{
    $routes->get('/', 'Admin\ComponentTypeController::index');
    $routes->get('create', 'Admin\ComponentTypeController::create');
    $routes->post('insert', 'Admin\ComponentTypeController::insert');
    $routes->get('edit/(:num)', 'Admin\ComponentTypeController::edit/$1');
    $routes->post('update/(:num)', 'Admin\ComponentTypeController::update/$1');
    $routes->get('delete/(:num)', 'Admin\ComponentTypeController::delete/$1');
});

```

## [app/Controllers/Admin/ComponentTypeController.php](/refactoring/app/Controllers/Admin/ComponentTypeController.php)

---
---


# plan 

## Fichiers à modifier
✅ [app/Libraries/Components/ComponentDefinition.php](/refactoring/app/Libraries/Components/ComponentDefinition.php)
- Ajout : toArray()
- Test :print_r($catalog->get('apex')->toArray());




2. app/Controllers/Api/ComponentCatalogController.php

Création
Responsabilité : instancier ComponentCatalog , retourner all() en JSON
Test : GET /api/component-catalog

Le navigateur doit afficher le JSON.

3. app/Config/Routes.php
Ajout GET /api/component-catalog

4.assets/js/core/ComponentCatalog.js
Modification
- Remplacer loadDefaults() par async load()

qui fait :
fetch(...)
↓
new ComponentDefinition(...)
↓
register()

Test

console.log(catalog.list());

doit retourner les composants.

5. assets/js/ui/workbench/catalog/ComponentCatalogWorkbench.js

Modification Dans bootstrap() :
await this.catalog.load();
avant
refreshCatalog();
Test

Le panneau de gauche affiche les composants.

Validation finale
✅ /cmptest fonctionne.
✅ /api/component-catalog retourne le JSON.
✅ console.log(catalog.list()) contient les composants.
✅ Le CatalogPanel affiche les composants.
✅ Un clic met à jour DefinitionPanel, DescriptorPanel et JsonPanel.

C'est tout. Aucun autre fichier n'est nécessaire pour terminer ce Stage 1.







