# ImageTagger



----
À modifier

app/Controllers/Api/Image.php
app/Controllers/Api/Mot.php
app/Config/Routes.php

À créer

app/Models/ImageMotModel.php
app/Services/ImageMotService.php

app/Controllers/Api/ImageMot.php
app/Controllers/Api/MotImage.php

---------------

fichiers
Image.php
Mot.php
Routes.php

ImageMotModel.php
ImageMotService.php


ImageMot.php
MotImage.php


-----------

Image.php                 
  │ include=mots

Le Controller reste responsable de la ressource Image.
Demande a ImageMotService les mots associés.

    GET /api/image/:id?include=mots
    
Lecture : GET /api/image/17/mots
    $routes->get( 'image/(:num)/mots', 'Api\ImageMot::index/$1');    

    GET /api/image/17?include=mots






---

Mot.php
│ include=images

    GET /api/mot/8/images

    GET /api/mot/:id?include=images
    
    GET /api/mot/8?include=images

Demande a ImageMotService les images associées.
    ImageMotService::getImages()


----

routes

/api/image/:id/mots -> ImageMot.php -> ImageMotService
/api/mot/:id/images -> MotImage.php -> ImageMotService

---
ImageMotModel.php
    représenter et manipuler la table pivot image_mot.

        image_mot
        ├── image_id
        └── mot_id

methodes

exists(imageId, motId)
attach(imageId, motId)
detach(imageId, motId)
motIdsForImage(imageId)
imageIdsForMot(motId)
deleteAllForImage(imageId)


ne pas modifier ImageMotModel.Il possède primitives dont ImageMotService a besoin
Le Controller ImageMot est suffisamment avancé. ne pas le réécrire


---
Controller ImageMot.php
Il appelle uniquement le Service.

    GET    /api/image/:id/mots
    $routes->get( 'mot/(:num)/images', 'Api\MotImage::index/$1' );

Ajout : POST /api/image/17/mots + {"mot_id": 8}
    POST   /api/image/:id/mots
    $routes->post( 'image/(:num)/mots', 'Api\ImageMot::attach/$1' );    
    
Suppression : DELETE /api/image/17/mots/8
    DELETE /api/image/:id/mots/:motId
    $routes->delete( 'image/(:num)/mots/(:num)' , 'Api\ImageMot::detach/$1/$2' );

---

Controller MotImage
    GET /api/mot/:id/images
    ImageMotService::getImages()
---

ImageMotService.php

Le service vérifie  : Image existe ? Mot existe ? Relation déjà présente ?Relation absente ?

getMots(int $imageId)
getImages(int $motId)
attach(int $imageId, int $motId)
detach(int $imageId, int $motId)


ImageMotService
       │
       ├── exists()
       ├── attach()
       ├── detach()
       ├── motIdsForImage()
       └── imageIdsForMot()
       └── deleteAllForImage()

deleteAllForImage() pourra être utile lors de la suppression d'une Image, même si la FK ON DELETE CASCADE doit normalement assurer cette responsabilité au niveau SQL.



-------------------------------------------------------------


Checklist:

1. ImageMotService::attach() — oui, modifier
2. getMots()
3. getImages() — à modifier également
4. Mot::batch() 


-----------
Contrats apres correction

ImageMotService
│
├── getMots(imageId)
│     → mot_id, mot_lbl
│
├── getImages(motId)
│     → id, filename, path, alt
│
├── attach(imageId, motId)
│     → image_id, mot_id, attached, already
│
├── detach(imageId, motId)
│     → bool
│
└── sync(imageId, motIds)
      → liste des mots finaux


---
UP fichiers

controllers
- Image.php
- Mot.php
- ImageMot.php
- MotImage.php   

Models
- ImageMotModel.php
Services
- ImageMotService.php


Routes.php   


```
CREATE TABLE image_mot (
    image_id INT UNSIGNED NOT NULL,
    mot_id   INT NOT NULL,

    PRIMARY KEY (image_id, mot_id),

    INDEX idx_image_mot_mot_id (mot_id),

    CONSTRAINT fk_image_mot_image
        FOREIGN KEY (image_id)
        REFERENCES images (id)
        ON DELETE CASCADE
        ON UPDATE CASCADE,

    CONSTRAINT fk_image_mot_mot
        FOREIGN KEY (mot_id)
        REFERENCES mots (mot_id)
        ON DELETE CASCADE
        ON UPDATE CASCADE
) ENGINE=InnoDB;

```


curl https://zealot.fr/api/image/1
=> ok
```
{
    "status": 200,
    "message": "OK",
    "data": {
        "id": "1",
        "user_id": "1",
        "width": "1024",
        "height": "1024",
        "ratio": "1.000",
        "extension": "jpg",
        "size_ko": "209.85",
        "path": "/assets/img/3js/skyrect/UnionSquare/negx.jpg",
        "filename": "negx.jpg",
        "alt": "",
        "status": "pending",
        "created_at": "2026-05-24 22:58:44",
        "updated_at": "2026-05-24 22:58:44"
    }
}
```

curl https://zealot.fr/api/image/10?include=mots
=> ok
```
{
    "status": 200,
    "message": "OK",
    "data": {
        "id": "10",
        "user_id": "1",
        "width": "2560",
        "height": "1600",
        "ratio": "1.600",
        "extension": "jpg",
        "size_ko": "213.20",
        "path": "/assets/img/3js/model3d/bateaux/barco/illustations/5.jpg",
        "filename": "5.jpg",
        "alt": "",
        "status": "pending",
        "created_at": "2026-05-24 22:58:44",
        "updated_at": "2026-05-24 22:58:44",
        "mots": [
            {
                "mot_id": "10",
                "mot_lbl": "abaissâmes"
            }
        ]
    }
}
```

curl https://zealot.fr/api/mot/10/images
=> ok
```
{
    "status": 200,
    "message": "OK",
    "data": [
        {
            "id": "10",
            "filename": "5.jpg",
            "path": "/assets/img/3js/model3d/bateaux/barco/illustations/5.jpg",
            "alt": ""
        }
    ]
}
```

curl https://zealot.fr/api/mot/batch?ids=1,3,5
=> ok
```js
{
    "status": 200,
    "message": "OK",
    "data": [
        {
            "mot_id": "1",
            "mot_lbl": "a"
        },
        {
            "mot_id": "3",
            "mot_lbl": "abaissa"
        },
        {
            "mot_id": "5",
            "mot_lbl": "abaissables"
        }
    ]
}
```



À tester encore pour ImageTagger
```
GET  /api/image?include=mot_ids            → grille avec badges  (index non testé)
GET  /api/image?status=pending             → filtre statut        (index non testé)
GET  /api/image/:id/mots                   → mots de l'image      (via ImageMot controller)
POST /api/image/:id/mots  {mot_id}         → attach
DELETE /api/image/:id/mots/:motId          → detach
PUT  /api/image/:id/mots  {ids:[...]}      → sync
GET  /api/mot/like?q=...                   → autocomplete
```



----



# routes

    $routes->get('imagetagger'          ,   'WorkbenchController::imagetagger'      );



# WorkbenchController
    public function imagetagger() { return view('workbench/imagetagger'); }  




# app/Views/workbench/imagetagger.php


fichiers

assets/js/ui/workbench/imagetagger/ImageTaggerListPanel.js
assets/js/ui/workbench/imagetagger/ImageTaggerWorkbench.js
assets/js/ui/workbench/imagetagger/TaggerPanel.js


/assets/css/workbench/workbench_tagger.css -> 
/assets/css/workbench/tagger.css



// assets/js/features/image/image.properties.js
// Correction iteration003 :
//   status : draft/published/archived → pending/validated/rejected (ImageModel)


// assets/js/features/image/image.service.js
// Iteration002 :
//   fetchImage   — ajout params status + include
//   fetchImageById — ajout param include
//   Status corrigés : pending | validated | rejected
// ─────────────────────────────────────────────────────────────────────────────


PAS FAIT
// app/Controllers/Api/Image.php  — MÉTHODES À AJOUTER / REMPLACER
// ─────────────────────────────────────────────────────────────────────────────
// Deux modifications par rapport à la version existante :
//
//   1. index()       — ajout ?status= + ?include=mot_ids
//   2. parseInclude  — whitelist étendue : ['mots', 'mot_ids']
//
// show() et les autres méthodes sont inchangées.



// assets/js/features/image/imageMot.service.js
renommer en assets/js/features/image/imagemot.service.js
modifier ImageTaggerWorkbench



// assets/js/features/mot/mot.service.js
// ─────────────────────────────────────────────────────────────────────────────
// Service Mot — new architecture.
// PK : mot_id (pas id) — tous les appels utilisent mot_id.
//
// fetchMot      — liste paginée (q, page, perPage)
// fetchMotLike  — autocomplete → items[] plat
// fetchMotBatch — multi-IDs en un appel → items[] plat (lazy load ImageTagger)
// saveMot       — POST (id=null) / PUT (id>0)
// deleteMot     — DELETE
// ─────────────────────────────────────────────────────────────────────────────



----



Deux problèmes distincts. 


Le CURL d'abord pour confirmer le 500.
problème l'appi necessite une authentification login apparait pour accéder à l'url : https://zealot.fr/workbench/imagetagger


CURL à lancer :

```
curl -X POST https://zealot.fr/api/image/5/mots \  -H "Content-Type: application/json" \  -d '{"mot_id":27800}'
```
reponds 500 - "Failed to parse JSON string. Error: Syntax error"


Diagnostic du 500 — sans attendre le CURL
 
 ```
https://zealot.fr/api/image/5/mots avec payload {"mot_id":27800}

POST /api/image/:id/mots  {mot_id}         → attach

 ```

est géré par 

```
    $routes->post( 'image/(:num)/mots', 'ImageMot::attach/$1');
```

Le message est explicite : There is no primary key defined when trying to make insert.

C'est CodeIgniter. ImageMotModel a $primaryKey = null (table pivot, clé composite).
Quand attach() appelle $this->insert(), le Model CI cherche une PK — il n'en trouve pas → 500.

J

 ```
<?php
//app/Models/ImageMotModel.php
namespace App\Models;

use CodeIgniter\Model;

/**
 * Pivot Image ↔ Mot — clé composite (image_id, mot_id).
 * Pas de CRUD ResourceController classique.
 */
class ImageMotModel extends Model
{
    protected $table            = 'image_mot';
    protected $primaryKey       = null; // composite — ne pas utiliser find($id)
    protected $useAutoIncrement = false;
    protected $returnType       = 'array';
    protected $allowedFields    = ['image_id', 'mot_id'];
    protected $useTimestamps    = false;

    public function exists(int $imageId, int $motId): bool
    {
        return $this
            ->where('image_id', $imageId)
            ->where('mot_id', $motId)
            ->countAllResults() > 0;
    }

    public function attach(int $imageId, int $motId): bool
    {
        if ($this->exists($imageId, $motId)) {
            return false; // déjà présent
        }

        return (bool) $this->insert([
            'image_id' => $imageId,
            'mot_id'   => $motId,
        ]);
    }

 ```



Le problème était uniquement dans le Controller.
Le Controller utilisait l'ancien contrat

Il cherche cette clé qui n'existe pas. $result['mot']

Dans app/Controllers/Api/ImageMot.php, il y avait :
```
if ($result['already']) {
    return $this->apiOk($result['mot'], null, 'Association déjà existante.');
}

return $this->apiCreated($result['mot'], 'Mot associé à l\'image.');
```

Le contrat actuel du Service est correct

ImageMotService::attach() retourne explicitement :

image_id
mot_id
attached
already

et pas mot. 


Dans app/Controllers/Api/ImageMot.php jai mis  :
```
if ($result['already']) {
    return $this->apiOk( $result, null, 'Association déjà existante.');
}

return $this->apiCreated( $result, 'Mot associé à l\'image.');
```
