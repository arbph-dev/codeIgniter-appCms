
Ordre de travail recommandé :

- [X] Documenter le process couche 5 (cette note) dans le repo
    - https://github.com/arbph-dev/codeIgniter-appCms/blob/main/project/daily/2026-09-01-002.md

- [ ] AdresseClient + mapping BAN → Zealot
    - [X] AdresseClient : https://github.com/arbph-dev/codeIgniter-appCms/blob/main/project/daily/2026-09-01-003.md#fichiers-1
    - [ ] mapping BAN → Zealot

- [ ] Brancher localisation dans le scoring (étape 2)

- [ ] EtablissementClient + étape 5 « lier adresse au siège »
    - https://github.com/arbph-dev/codeIgniter-appCms/blob/main/project/daily/2026-08-31-001-Etablissement.md
    

- [ ] Orphelines côté serveur + attach idempotent


- [ ] Services d'entreprise et d'organisation
    - https://github.com/arbph-dev/codeIgniter-appCms/blob/main/documentation/METIERS/ORGANISATIONS/service_types.md
        - [ ] Backend :Table, Model (entities) , Service , Controller , Routes ou Enums (attention js)
    - https://github.com/arbph-dev/codeIgniter-appCms/blob/main/documentation/METIERS/ORGANISATIONS/services.md
        - [ ] Backend :Table, Model (entities) , Service , Controller , Routes
        - [ ] ServorgClient

- Ressources
    - Refactor Organisation Entreprises Etablissements
        - https://github.com/arbph-dev/codeIgniter-appCms/blob/main/project/daily/2026-08-30.md 
    
---

### Image
Fin des tests
    [2026-09-01-004.md](/project/daily/2026-09-01-004.md)



-----------------------------------------------------------------------------------------

## 2026-09-02-002

fichier 
cli\menus\layer5.py

main6_light.py

cli\menu.py
cli\menus\layer5.py
[cli/layer5/etapes.py](/Orbis/cli/layer5/etapes.py)
cli\layer5\scoring.py
cli\layer5\working_memory.py
services\api\entreprise_client.py
services\api\insee_client.py
acquisition\sources.py
transformation\mapper.py
persistence\models.py
persistence\repository.py

------
2026-09-02-002

fichiers
- [X] creer cli/layer5/text_utils.py 
- [X] créer cli/layer5/insee_enrich.py
- [X] update partiel pour acquisition\sources.py 
- [X] cli/layer5/scoring.py https://github.com/arbph-dev/codeIgniter-appCms/blob/main/Orbis/cli/layer5/scoring.py 
    - [X] update 147/147
    - [X] retirer : _norm_upper(), _tokens()
- [ ] update cli/layer5/etapes.py
    - [X] import 
    - [X] modifier  etape2_insee + _run_etape2
    - [X] show_wm — afficher loc
- [ ] working_memory.py — ajouter les champs sur WMRecord :

#### cli/layer5/scoring.py 
- intégrer cli/layer5/text_utils.py (import)
- remplacer les fonctions locales par l'import :

#### cli/layer5/etapes.py
- **import**
```
+ rich ( Confirm)
+ from cli.layer5.insee_enrich import enrich_candidates_siege
```

- **modifier**  etape2_insee + _run_etape2
- remplacer _normalize_insee_denom locale :

etapes_V2.py — affichage enrichi (résumé + show_wm) :

---

A corriger
- Duplication _normalize_insee_denom / _norm_upper toujours présente.
- Pas de try/except autour de insee.search_siren() lui-même dans la boucle principale de _run_etape2.

match_pct = moyenne de tous les candidats, pas le score du meilleur 
- On corrige le top et on améliore les stats : min, max , moy


Pour le rate limite, point le plus génant :
cela fait partie des automatismes et regle a implementer peut etre dans la couche 6 , la couche 7 devenant l'ui

Ici on a une base a qualifier pas un besoin ponctuel donc une tache planifiée
- on designe l'API a améliorer : zealot "Entreprise" (Organisation/Entreprise/Etablsisement)

- on défini une plage d'id 1 à 124 (broken or dirty import ); on pourrait selectionner les ids avec l'ui + tard mais en ponctuel

- on défini un timer, on loop sur le timer until range is empty

---



#### cli/layer5/text_utils.py 

On mutualise la normalisation de texte pour que etapes_V2.py et scoring_V2.py partagent la même logique
```
def norm_upper(s: str) -> str:
    Minuscules → ASCII approx → MAJUSCULES, apostrophes neutralisées, espaces compactés.

def tokens(s: str) -> list[str]:

def normalize_insee_denom(nom: str) -> str:    
    Alias explicite pour l'usage côté requête Lucene INSEE.
```

**implique**
- scoring_V2.py - intégrer cli/layer5/text_utils.py  — remplacer les fonctions locales par l'import :
- etapes_V2.py — remplacer _normalize_insee_denom locale :

#### cli/layer5/insee_enrich.py
ras

## 2. match_pct = top candidat, + stats min/max/moy

#### working_memory.py
ajouter les champs sur WMRecord :

```python
@dataclass
class WMRecord:
    ...
    match_pct: int = 0        # devient le score du TOP candidat
    match_min: int = 0
    match_max: int = 0
    match_moy: int = 0
    veracity_pct: int = 0
    global_pct: int = 0
```
scoring_V2.py — score_record() :

```python
def score_record(rec: WMRecord) -> None:
    scored = [score_candidate(rec, c) for c in rec.insee_candidates]
    scored.sort(key=lambda x: x.score_pct, reverse=True)
    rec.scored = scored

    if scored:
        pcts = [s.score_pct for s in scored]
        rec.match_pct = pcts[0]                      # top, plus la moyenne
        rec.match_max = pcts[0]                       # == top (liste triée desc)
        rec.match_min = min(pcts)
        rec.match_moy = int(round(sum(pcts) / len(pcts)))
    else:
        rec.match_pct = rec.match_min = rec.match_max = rec.match_moy = 0

    v = 0.0
    if rec.siren or (rec.siret and len(re.sub(r"\D", "", rec.siret or "")) >= 9):
        v += 1.0 / 3.0
    if scored:
        v += 2.0 / 3.0
    rec.veracity_pct = int(round(100.0 * v))
    rec.global_pct = int(round((rec.match_pct * 2 + rec.veracity_pct) / 3))
```

etapes_V2.py — affichage enrichi (résumé + show_wm) :

```python
# dans _run_etape2, colonne "M/V/G" →
f"M{rec.match_pct}(min{rec.match_min}/moy{rec.match_moy}) V{rec.veracity_pct} G{rec.global_pct}",

# dans show_wm() :
console.print(
    f"  org#{r.organisation_id} {r.nom!r} "
    f"siren={r.siren or '—'} status={r.status} "
    f"M{r.match_pct}(min{r.match_min}/moy{r.match_moy}/max{r.match_max}) "
    f"V{r.veracity_pct} G{r.global_pct}"
)

```
## 3. try/except autour de insee.search_siren()
etapes_V2.py

Dans _run_etape2, entourer l'appel réseau lui-même — sinon une seule erreur réseau perd la progression de tous les records restants :

```python
console.print(f"[dim]→ org#{rec.organisation_id}  q={q!r}[/]")
try:
    data = insee.search_siren(q, nombre=max_per_org)
except Exception as e:
    console.print(f"[red]  INSEE search_siren échec org#{rec.organisation_id}: {e}[/]")
    data = None
    rec.status = "error"
    # on continue quand même le scoring (candidates vide) plutôt que de tout arrêter
```

Et faire le même geste sur le fallback (data2 = insee.search_siret(...)) puisqu'il partage le même risque.







    
