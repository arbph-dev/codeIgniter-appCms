# Authentification et Workbench

La vue actuelle(old) du portail, [app/Views/cms/index.php](https://github.com/arbph-dev/codeIgniter-appCms/blob/main/old/app/Views/cms/index.php#L258), utilise un layout 

Le layout dispose d'une section `header` 
- embarque le formulaire d'authentification
- les infomrations de l'user ssi authentifié

cette version SSR avec du script était géré coté serveur

Demain c'est le script front qui devra s'en chargé. 


les vues futures (refactoring) dans refactoring/app/
- https://github.com/arbph-dev/codeIgniter-appCms/blob/main/refactoring/app/Views/workbench/

une vue en test avant production dans refactoring/app/
- https://github.com/arbph-dev/codeIgniter-appCms/blob/main/refactoring/app/Views/workbench/adresse.php


Pourquoi le découplage est important : aujourd'hui ci4 demain laravel ou meme django 
donc on gardera les routes et les workbench, il y aura certainement la partie auth a revoir mais c'est tout

Maintenant reste le problème du crsf à voir lors des test de validation
La séance visera a sécuriser le serveur qui va passer en production -> plus de toolbar CI4
les mots de passe seront remplacé et les comptes de test supprimés, users  ssh, smtp, db, ftp 

## Bilan

| Action             | Fichier                                                                                                             |
| ------------------ | ------------------------------------------------------------------------------------------------------------------- |
| ✅ Aucun changement | `apiFetch.js`, `auth.controller.js`, `auth.store.js`, `auth.service.js`, `AuthController.php`, tous les Workbenches |
| 🆕 Créer           | `assets/js/wbapp.js`                                                                                                  |
| 🆕 Créer           | `assets/js/ui/workbench/core/AuthPanelBase.js`                                                                          |
| 🔧 Modifier - normalisation non prioritaire        | `assets/js/features/auth/auth.controller.js`                                             |
| 🔧 Modifier          | `app/Views/workbench/organisation.php`                                                                              |
| 🔧 Modifier          | `app/Views/workbench/adresse.php`                                                                                   |
| 🔧 Modifier         | `app/Views/workbench/mot.php`                                                                                       |
| 🔧 Modifier        | `app/Config/Routes.php` — routes + filtres Shield                                                                   |



### Actions
- modifier `.env`
- créer les fichiers
- modifier adresse pour valider


## fichiers :
- 🆕 [/assets/js/ui/workbench/core/AuthPanelBase.js](https://github.com/arbph-dev/codeIgniter-appCms/blob/main/refactoring/assets/js/ui/workbench/core/AuthPanelBase.js)
- 🆕 [/assets/js/ui/workbench/auth/ToolbarAuthPanel.js](https://github.com/arbph-dev/codeIgniter-appCms/blob/main/refactoring/assets/js/ui/workbench/auth/ToolbarAuthPanel.js)
- 🆕 [/assets/js/wbapp.js](https://github.com/arbph-dev/codeIgniter-appCms/blob/main/refactoring/assets/js/wbapp.js)
- 🔧 [/app/Views/workbench/adresse.php]()






## AuthPanelBase
Base des panels d'authentification.

Différence avec PanelBase / ListPanelBase :
- Ne crée PAS son container — il prend le contrôle d'un élément existant
- render() est remplacé par init(selector) — pattern WorkbenchBase
- Le bus est la seule source d'état — pas de show(data) externe

// Contrat sous-classes :
`_buildLoading()`           → HTMLElement
`_buildGuestForm(error)`    → HTMLElement
`_buildUserBar(user)`       → HTMLElement[]

- aucun appel API
- aucun import authStore
- aucune logique métier auth

**dépendances**
```js
import PanelBase   from '/assets/js/ui/workbench/core/PanelBase.js'
import { bus }     from '/assets/js/core/eventBus.js'
import { clear }   from '/assets/js/core/domhelper.js'
```

---

## ToolbarAuthPanel 
Panel auth de la barre de navigation (header) -> cible `header/div.header-auth`

- ToolbarAuthPanel **(extends AuthPanelBase)**
- Implémente les builders DOM de AuthPanelBase via create().
 
Ne contient aucune logique — uniquement de la présentation.

**dépendances**
```js
import AuthPanelBase from '/assets/js/ui/workbench/core/AuthPanelBase.js'
import { create }    from '/assets/js/core/domhelper.js'
```

---

## wbapp.js
Couche application — orchestre auth et cycle de vie des Workbenches.

**A NOTER**
app.js peut charger plusieurs workbenches c'est une piste très intéressante, a tester et valider avec layout et section

**dépendances**
modifier les url -> `/assets/js/`
```js
import { bus }                from './core/eventBus.js'
import { initAuthController } from './features/auth/auth.controller.js'
import ToolbarAuthPanel       from './ui/workbench/auth/ToolbarAuthPanel.js' // new
```



## 🔧 assets/js/features/auth/auth.controller.js

`AuthPanelBase.js` , `ToolbarAuthPanel.js` et `wbapp.js` n'utilisent pas assets/js/features/auth/index.js

`auth.controller.js` contient les imports `eventBus.js` , `auth.store.js` et `auth.service.js`

**dépendances**
modifier les url -> `/assets/js/`
```
import { bus }                          from '../../core/eventBus.js'
import { authStore }                    from './auth.store.js'
import { fetchLogin, fetchMe, fetchLogout } from './auth.service.js'
```



# intégration
La vue est une page HTML autonome => pas de layout, pas de PHP auth. 
- un element header avec un child div de classe `.header-auth` va **dans la vue**
- `wbapp.js` le remplit.

`wbapp.js` fait tout :
- `ToolbarAuthPanel` cible `.header-auth` et le remplit
- `auth:success` → monte `AdresseWorkbench` sur `#adresseWorkbench`

L'inline script disparaît. 
```html
  <!--
    <script type="module">
        import AdressseWorkbench from '/assets/js/ui/workbench/adresse/AdresseWorkbench.js';
        const wb = new AdressseWorkbench();
        await wb.init('#adresseWorkbench');
    </script>
  -->
```

La vue ne connaît ni l'auth ni le workbench — juste les divs et les styles.

```html
<?php // app/Views/workbench/adresse.php ?>
<!DOCTYPE html>
<html lang="fr">
<head> ...</head>
<body>

    <header>
        <div class="header-auth"></div>
    </header>

    <div id="adresseWorkbench" data-workbench="adresse"></div>

    <script type="module" src="/assets/js/wbapp.js"></script>

</body>
</html>
```

## Tests
2 modifications
- app/Views/workbench/adresse.php / ligne 30
```diff
- <div id="adresseWorkbench" data-workbench="adresse"></div>
+ <div id="AdresseWorkbench" data-workbench="adresse"></div>
```
- assets/js/wbapp.js /  ligne 39
```diff
-   `/assets/js/ui/workbench/${type}.js`
+  `/assets/js/ui/workbench/${type}/${el.id}.js`

ceci impose 2 conventions de notation
```
XxxWorkbench 
- est dans un dossier xxx, le nom de dossier est inclue dans la balise ARIA
- le nom du fichier XxxWorkbench.js est employé dans l'attribut id 
```

exemple AdresseWorkbench : assets/js/ui/workbench/adresse/AdresseWorkbench.js

un dossier adresse contenant le fichier AdresseWorkbench.js =>  `<div id="AdresseWorkbench" data-workbench="adresse"></div>`









