
#### 2026-09-02-001-A

Source	Cible
AdresseBan (acquisition/sources) -> body AdresseClient.create(...)
BAN libre (label, street, postcode…) -> schéma normalisé Zealot (voienom, codepostal_id, …)

Champs Zealot obligatoires à la création : 
voienom, codepostal_id.

Référentiels à résoudre : 
CodesPostauxClient / TypeVoieClient (ou noms exacts de tes clients).

#### transformation/adresse_mapper.py
Couche 3 — AdresseBan (BAN) → payload Zealot /adresse.
- Ne fait aucun HTTP : les ids référentiels sont passés en argument ou résolus par l'appelant avant create.


#### services/api/adresse_from_ban.py 
Orchestration couche 2+3 
- Résolution des FK puis create — à appeler depuis le menu ou layer5

BAN → résolution référentiels Zealot → POST /adresse.

Dépend de :
- BanClient (déjà) → AdresseBan
- CodePostalClient / TypeVoieClient
- AdresseClient.create
- transformation.adresse_mapper.ban_to_zealot_payload


--------------------------------


Adapte les noms CodePostalClient / méthodes (like, get_by_codepostal) à tes fichiers 

Fichier : menu CLI (option dans cli/menus/adresse.py)
```Python
# cli/menus/adresse.py  — item « Créer depuis BAN »
# 1) BanClient.search(q) → liste AdresseBan
# 2) user choisit un résultat
# 3) create_adresse_from_ban(ban, adresse_client, cp_client, tv_client)
# 4) affiche id Zealot + ban_id
```
Prérequis backend (rappel)

Colonne adresses.ban_id + allowedFields / buildPayload
Index non unique sur ban_id


Enchaînement couche 5 (après mapping)
textÉtape 4 attach (siret siège)
  → INSEE/SIRET ou label connu
  → BanClient.search / reverse
  → create_adresse_from_ban
  → EtablissementClient.update(siege_id, adresse_id=…)
  → (optionnel) OrganisationClient.update(org_id, adresse_id=…)
Scoring : WMRecord.localisation = f"{postcode} {city}" dès qu’une adresse org ou un hit BAN est connu → activer W_LOC.

Checklist mapping

Vérifier ban_id en base + API POST
Coller transformation/adresse_mapper.py
Coller services/api/adresse_from_ban.py (ajuster clients CP / type voie)
Test manuel : BAN « 12 rue de … 29000 Quimper » → POST adresse → GET avec ligne4
Brancher un item menu Adresses « Depuis BAN »
Puis scoring localisation + EtablissementClient

Envoie si besoin les signatures exactes de codepostal_client / typevoie_client / BanClient.search pour caler resolve_* au pixel près.


---

#### 2026-09-02-001-B

Peut tu me donner les diff pour

codepostal_client.py  : https://github.com/arbph-dev/codeIgniter-appCms/blob/main/Orbis/services/api/codepostal_client.py

typevoie_client.py :  https://github.com/arbph-dev/codeIgniter-appCms/blob/main/Orbis/services/api/typevoie_client.py

cli/menus/adresse.py :  https://github.com/arbph-dev/codeIgniter-appCms/blob/main/Orbis/cli/menus/adresse.py




Voici des diffs ciblés calés sur tes clients actuels (find_by_codepostal, like, etc.).

- [X] Orbis/services/api/codepostal_client.py : Ajouter `resolve_id`
    - codepostal BAN (+ commune optionnelle) → codes_postaux.id
    - Un même CP peut couvrir plusieurs communes : si commune est fournie, on privilégie le match sur le libellé commune.

- [X] Orbis/services/api/typevoie_client.py
    - Ajouter `def resolve_id`


- [X] Orbis/transformation/adresse_mapper.py
    - Pas un diff : fichier à créer (mapping pur, sans HTTP) — voir message précédent ban_to_zealot_payload.
    - ban_to_zealot_payload (rappel minimal)

- [X] Orbis/services/api/adresse_from_ban.py

5. Orbis/cli/menus/adresse.py
    - import
    - Dans _menu_adresses, ajouter l’entrée de menu "Créer depuis BAN"
    - Ajouter le handler et modifier les choix
    - Ajouter Fonction _create_from_ban

----

Eviter la confusion dans le dry-run, dans _create_from_ban => forcer l’affichage des valeurs :

```Python
# cli/menus/adresse.py — après create_adresse_from_ban
console.print("[bold]payload[/]")
for k, v in result["payload"].items():
    console.print(f"  {k}: {v!r}")
# optionnel : voir_detail(result["payload"])  # tree = choix 1
```


----

1. Objectif de l’étape 2
Pour chaque WMRecord déjà en WorkingMemory (après étape 1) :

Construire une requête Sirene conforme
Récupérer jusqu’à N unités légales INSEE
Les parser en EntrepriseInsee
Scorer chaque candidat vs l’org (nom, siren, loc, naf, …)
Calculer M / V / G sur le record
Afficher le résumé (top candidat trié par score)

---

## 2026-09-02-001-C


cli/layer5/insee_enrich.py
    créer
* acquisition/sources.py
    localisation: Optional[str] = None sur EntrepriseInsee

* cli/layer5/scoring.py
    remplacer (score_loc branché)

cli/layer5/etapes.py
    étape 2 + show_wm comme ci-dessus
