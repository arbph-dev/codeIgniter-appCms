Nous avons désormais deux familles de composants.

Les **composants passifs** qui génèrent uniquement du HTML.
- Raw
- Callout

Les **composants actifs** qui possedent un état , un cycle de vie , des assets et  une API.
- Apex
- Leaflet
- Mermaid
- CodeVal
- ThreeJS

---
# Leaflet

Leaflet est le premier composant qui cumule presque toutes les difficultés :

ressources CSS externes ;
bibliothèques JS externes ;
plugin IGN ;
fichier customConfig.json ;
initialisation asynchrone ;
problème de visibilité (display:none) ;
plusieurs couches ;
état interne (map, layers, markers, etc.).

Si on arrive à intégrer Leaflet proprement sans casser l'architecture, c'est que les conventions sont bonnes.

## feuille de route

### Étape 1 — Créer le composant Leaflet minimal

#### 1.1 – Les fichiers

à créer :
- app/Libraries/Components/Renderers/LeafletRenderer.php
- app/Libraries/Components/AdminRenderers/LeafletAdminRenderer.php
- assets/js/components/leaflet.js

à modifier:
- assets/js/cms/bootstrap.js
- DescriptorMapper
- component_types sql
- cmspart controleur
- app/Views/cms/article.php

##### 1.1.1 - LeafletRenderer.php

utiliser des data-* plutôt qu'un JSON complet pour cette première itération :
ils sont lisibles directement dans le DOM ;
faciles à déboguer avec les outils du navigateur ;
simples à récupérer en JavaScript (el.dataset.lat, etc.) ;
cohérents avec ce que fait déjà ApexRenderer.

Le jour où Leaflet devra gérer des objets plus complexes (liste de couches, marqueurs, styles, GeoJSON...), on pourra passer à un unique attribut :
```
data-config='{"lat":47.82,"lng":-4.30,"layers":["osm"]}'
```

---

##### 1.1.2 - LeafletAdminRenderer.php

rester sur la même philosophie qu'ApexAdminRenderer : 

un formulaire minimal mais complet. Le but est uniquement de pouvoir configurer une carte OSM.

quatre champs correspondent exactement au LeafletRenderer :
config[id]
config[lat]
config[lng]
config[zoom]

On évite d'introduire dès maintenant :
- les couches ;
les marqueurs ;
les fonds de carte ;
les plugins IGN ;
les options Leaflet.
Une petite amélioration

**profiter de Leaflet pour améliorer tous les futurs AdminRenderer**.

Plutôt que :
```
$lat = $descriptor->get('lat', 47.82);
```
je définirais des constantes dans la classe :
```
protected const DEFAULT_LAT  = 47.82;
protected const DEFAULT_LNG  = -4.30;
protected const DEFAULT_ZOOM = 11;
```
Puis :
```
$lat  = $descriptor->get('lat', self::DEFAULT_LAT);
$lng  = $descriptor->get('lng', self::DEFAULT_LNG);
$zoom = $descriptor->get('zoom', self::DEFAULT_ZOOM);
```
Ainsi, les valeurs par défaut sont centralisées et faciles à modifier (par exemple si tu souhaites centrer par défaut les cartes sur une autre région). Nous pourrons généraliser cette pratique aux autres composants lors de leur refactorisation.



##### 1.1.3 Leaflet.js

On écrit le premier composant Leaflet comme si on n'avait jamais développé de carte auparavant.

Objectif :

une carte OSM ;
un seul type de carte ;
un seul event (leaflet:render) ;
aucune dépendance IGN ;
aucune notion de visibilité (pour l'instant).

Le fichier est volontairement très proche d'apex.js afin que les deux composants soient comparables après coup.
Elle est presque un copier-coller conceptuel d'Apex :

```
ENGINE
↓
REGISTRY
↓
RENDERER
↓
BOOTSTRAP
↓
API
```

Ce que cette version apporte déjà
✔ Même architecture que apex.js.
✔ ENGINE / REGISTRY / RENDERER / BOOTSTRAP / INDEX / API.
✔ Une seule responsabilité par fonction (createMap, createTileLayer, renderMap...).
✔ Gestion des instances (Map instances).
✔ update, destroy et list déjà présents.
✔ invalidateSize() pour préparer le futur cycle de vie des composants.

Je pense que cette base est suffisamment propre pour servir de modèle. L'étape suivante sera d'intégrer progressivement les fonctionnalités de ton ancien mapleaflet.js (IGN, SHOM, LayerSwitcher, GeoPortail, gestion des composants cachés, etc.) sans remettre en cause cette architecture. C'est à ce moment-là que nous pourrons vérifier si les conventions sont suffisamment générales pour être réutilisées avec Three.js.

---

##### 1.1.4 bootstrap.js

Son évolution vers l'emploi d'un [componentRegistry](/old/public/assets/js/core/componentRegistry.js) devient nécessaire.

Ajouter un composant ne nécessitera plus de modifier le bootstrap : il s'enregistrera dans le componentRegistry

il faut considérer l'ordre de dépendance
- CodeVal publie des événements apex:render, donc Apex doit être initialisé avant.
- On prévoit de faire de meme avec d'autres composants; CodeVal est intilalisé en dernier

- assets/js/cms/bootstrap.js

---

##### 1.1.6 ComponentRegistry
```php
use App\Libraries\Components\Renderers\LeafletRenderer;
```

```php
protected array $renderers = [
    'raw'      => RawRenderer::class,
    'apex'     => ApexRenderer::class,
    'codeval'  => CodeValRenderer::class,
    'mermaid'  => MermaidRenderer::class,
    'callout'  => CalloutRenderer::class,
    'leaflet'  => LeafletRenderer::class,
];
```

---

##### 1.1.7 AdminComponentRegistry
```php
use App\Libraries\Components\AdminRenderers\LeafletAdminRenderer;
```

```php
protected array $renderers = [
    'raw'      => RawAdminRenderer::class,
    'apex'     => ApexAdminRenderer::class,
    'codeval'  => CodeValAdminRenderer::class,
    'mermaid'  => MermaidAdminRenderer::class,
    'callout'  => CalloutAdminRenderer::class,
    'leaflet'  => LeafletAdminRenderer::class,
];
```

---

##### 1.1.8 DescriptorMapper
Ajouter le nouveau type.

```php
protected array $types =
[
    ...
    6 => 'leaflet',
];
```

---

##### 1.1.9 table component_types

```sql
INSERT INTO component_types
(
    id,
    name,
    view,
    description,
    is_active,
    created_at
)
VALUES
(
    6,
    'leaflet',
    'components/leaflet',
    'Carte Leaflet / OpenStreetMap',
    1,
    NOW()
);
```

---

##### 1.1.10 table cmsparts

créer une Part Leaflet dans la table cmsparts 

Avec :
- section_id = 999 section test
- type_id = 6
- position = 8
- is_published = 1
- config:
```json
{ "id":"MAP_1", "lat":47.82, "lng":-4.30, "zoom":11 }
```

essai : https://zealot.fr/cms/article/test-art
on visualise le code
```html
<div class="cms_part_content">
        <div id="MAP_1" class="cp_leaflet" data-lat="47.82" data-lng="-4.3" data-zoom="11">
	</div>
</div>
```
une erreur est présente il faut encore importer leaflet, et le css.

---

##### 1.1.11 vue cms/article

Il manque les bibliothèques Leaflet.

Dans app/Views/cms/article.php, ajouter le module Leaflet via CDN, le CSS avant cms/bootstrap.js:
```html
<link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css">
<script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
```

###### CSS dédié
Créer /assets/css/components/leaflet.css avec la mise en page du composant.

###### Support de plusieurs cartes
Créer deux autres Parts Leaflet sur la même page et vérifier que : MAP_1 , MAP_2 et MAP_3 s'affiche correctement.




##### 1.1.12 - Phase 1 / Test 1 

Depuis la console , Vérifier :

- leafletList()
```js
leafletList()
```
- destroy()
```js
leafletDestroy("MAP_1")
```
- render()
La carte doit disparaître proprement.
Elle doit recréer une carte centrée sur Paris.
```js
leafletRender("MAP_1",{ lat:48.8566 , lng:2.3522 , zoom:10 } )
```



---
---


ComponentRegistry

Étape 2 — Ajouter les assets
Étape 3 — Ajouter Geoportail
Étape 4 — Ajouter les couches
Étape 5 — Ajouter les marqueurs
Étape 6 — Ajouter l'AdminRenderer
Étape 7 — Refactoriser le Registry

Une fois cette étape validée, nous pourrons commencer à réintroduire progressivement :

le plugin IGN ;
les couches WMTS ;
le LayerSwitcher ;
les marqueurs ;
les GeoJSON ;
la gestion des composants cachés (invalidateSize et futur cycle de vie).
